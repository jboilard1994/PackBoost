[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "packboost"
version = "1.0"
dependencies = ["torch", "scikit-learn", "numpy"]

[project.optional-dependencies]
dev = [
  "pytest",
]
